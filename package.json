{
  "name": "sme-fund-platform",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/platform"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @prosme/platform",
    "build": "npm run build --workspace @prosme/platform",
    "start": "npm run start --workspace @prosme/platform",
    "lint": "npm run lint --workspace @prosme/platform",
    "typecheck": "npm run typecheck --workspace @prosme/platform",
    "test": "npm run test --workspace @prosme/platform",
    "verify:g1:firebase": "node scripts/development/verify-firebase-g1.mjs",
    "check:files": "npm run check:files --workspace @prosme/platform",
    "check": "npm run check:files && npm run lint && npm run typecheck && npm run test && npm run build"
  }
}
